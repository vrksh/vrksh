{
  "name": "vrksh-site",
  "private": true,
  "scripts": {
    "build": "npx @tailwindcss/cli -i assets/css/main.css -o static/css/main.css --minify && hugo --minify",
    "postbuild": "npx pagefind --site public",
    "dev": "hugo server"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "pagefind": "^1.3.0"
  }
}
