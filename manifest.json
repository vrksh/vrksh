{
  "version": "0.1.0",
  "tools": [
    {"name": "jwt",    "description": "JWT inspector — decode, --claim, --expired"},
    {"name": "epoch",  "description": "Timestamp converter — unix↔ISO, relative time"},
    {"name": "uuid",   "description": "UUID generator — v4/v7, --count, --json"},
    {"name": "tok",    "description": "Token counter — --budget, --fail, --model"},
    {"name": "sse",    "description": "SSE stream parser — text/event-stream to JSONL"},
    {"name": "coax",   "description": "Retry wrapper — --times, --backoff, --on, --until"},
    {"name": "prompt", "description": "LLM prompt — --model, --budget, --schema, --fail"},
    {"name": "kv",     "description": "Key-value store — set/get/del/list, SQLite-backed"},
    {"name": "chunk",  "description": "Token-aware text splitter — JSONL chunks within a token budget"},
    {"name": "grab",   "description": "URL fetcher — clean markdown, plain text, or raw HTML"},
    {"name": "links",  "description": "Hyperlink extractor — markdown, HTML, bare URLs to JSONL"},
    {"name": "plain",    "description": "Markdown stripper — removes syntax, keeps prose"},
    {"name": "validate", "description": "JSONL schema validator — --schema, --strict, --fix, --json"},
    {"name": "mask",     "description": "Secret redactor — entropy + pattern-based, streaming"}
  ]
}
