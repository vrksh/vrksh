name: moniker
tagline: Memorable name generator - run IDs, job labels, temp dirs
description: |
  Generate adjective-noun names from a curated word list. Deterministic
  with --seed. Within-run uniqueness via Fisher-Yates shuffle. Add words
  with --words for lower collision probability.
group: v1
category: utilities
example: vrk moniker --seed 42
flags:
  - flag: --count
    short: "-n"
    type: int
    description: Number of names to generate
  - flag: --separator
    short: ""
    type: string
    description: Word separator
  - flag: --words
    short: ""
    type: int
    description: Words per name (minimum 2)
  - flag: --seed
    short: ""
    type: int64
    description: Random seed for deterministic output
  - flag: --json
    short: "-j"
    type: bool
    description: "Emit JSON per name: {name, words}"
  - flag: --quiet
    short: "-q"
    type: bool
    description: Suppress stderr output
exit_codes:
  - code: 0
    meaning: Success
  - code: 1
    meaning: Word pool exhausted for requested count
  - code: 2
    meaning: --count less than 1, --words less than 2
og_image:
  headline: "Generate memorable names. For pipelines."
  code: "vrk moniker --seed 42"
mcp_callable: true
