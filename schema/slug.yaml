name: slug
tagline: URL/filename slug generator - --separator, --max, --json
meta_description: "Generate URL-safe slugs from any text. Handles unicode, custom separators, and length limits."
og_title: "vrk slug - URL and filename slug generator"
description: |
  You need URL-safe slugs from user-provided titles. You lowercase, replace spaces with hyphens, strip special characters. Then a user submits a title with Unicode characters, curly quotes, or unusual punctuation. Your regex misses them. The slug breaks the URL.

  `vrk slug` converts any text to a URL-safe, filename-safe slug. Normalizes Unicode to ASCII, lowercases everything, replaces non-alphanumeric characters with hyphens, and collapses consecutive hyphens. Use `--max` to truncate at word boundaries for length-limited contexts.
problem: >
  You generate filenames from document titles. A title contains "Uber's $3.1B
  Q4 Revenue" and your slugify function produces "uber-s--3-1b-q4-revenue"
  with a double hyphen. Another title has Unicode characters that pass through
  unchanged and break a downstream system that expects ASCII-only paths.
before: |
  echo "$TITLE" | tr '[:upper:]' '[:lower:]' | tr ' ' '-' | tr -cd 'a-z0-9-'
  # Misses Unicode normalization, doesn't collapse double hyphens
after: "echo $TITLE | vrk slug"
group: v1
category: utilities
example: "echo 'My Blog Post: A Deep Dive into LLM Pipelines!' | vrk slug"
flags:
  - flag: --separator
    short: ""
    type: string
    description: Word separator character or string
  - flag: --max
    short: ""
    type: int
    description: Max output length; truncated at last separator (0 = no limit)
  - flag: --json
    short: "-j"
    type: bool
    description: "Emit JSON per line: {input, output}"
  - flag: --quiet
    short: "-q"
    type: bool
    description: Suppress stderr output
exit_codes:
  - code: 0
    meaning: Success
  - code: 1
    meaning: Runtime error (I/O failure)
  - code: 2
    meaning: Interactive TTY with no stdin
og_image:
  headline: "Slugify anything. URL-safe, Unicode-aware."
  code: "echo 'My Article (2024)' | vrk slug"
mcp_callable: true
