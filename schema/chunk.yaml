name: chunk
tagline: Token-aware text splitter - JSONL chunks within a token budget
description: |
  Split text into token-bounded chunks for processing within LLM context
  windows. Each chunk is a JSONL record with index, text, and exact token
  count. Supports paragraph-aware splitting and overlap for context continuity.
group: v1
example: cat long-doc.md | vrk chunk --size 4000
flags:
  - flag: --size
    short: ""
    type: int
    description: Max tokens per chunk (required)
  - flag: --overlap
    short: ""
    type: int
    description: Token overlap between adjacent chunks
  - flag: --by
    short: ""
    type: string
    description: "Chunking strategy: paragraph"
  - flag: --quiet
    short: "-q"
    type: bool
    description: Suppress stderr output
exit_codes:
  - code: 0
    meaning: Success, including empty input
  - code: 1
    meaning: I/O error
  - code: 2
    meaning: No input, --size missing or < 1, --overlap >= --size, unknown flag
og_image:
  headline: "Split documents. Stay within token budgets."
  code: "cat long-doc.md | vrk chunk --size 4000"
mcp_callable: true
