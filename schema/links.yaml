name: links
tagline: hyperlink extractor - markdown, HTML, bare URLs to JSONL
description: |
  Extract all links from markdown, HTML, or plain text. Each link becomes
  a JSONL record with text, URL, and source line number. Handles inline
  markdown, reference-style, HTML anchors, and bare URLs in a single pass.
group: v1
category: utilities
example: cat README.md | vrk links
flags:
  - flag: --bare
    short: "-b"
    type: bool
    description: Output URLs only, one per line
  - flag: --json
    short: "-j"
    type: bool
    description: Append metadata trailer after all records
  - flag: --quiet
    short: "-q"
    type: bool
    description: Suppress stderr output
exit_codes:
  - code: 0
    meaning: Success, including empty input and documents with no links
  - code: 1
    meaning: I/O error reading stdin
  - code: 2
    meaning: Interactive TTY with no piped input, unknown flag
og_image:
  headline: "Extract every link. Markdown, HTML, bare URLs."
  code: "cat README.md | vrk links --bare"
mcp_callable: true
